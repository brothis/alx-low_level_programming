#include <stdio.h>
/**
 * main - A program that prints various types
 * Return 0 (pass)
 */
int main(void)
{
	char a;
	int b;
	long int c;
	long long int d;
	float f;
printf("Size of a char: %lu bytes(s)\n", (unsigned long)sizeof(a));
}
